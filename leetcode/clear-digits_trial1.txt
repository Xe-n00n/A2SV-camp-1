class Solution:
    def clearDigits(self, s: str) -> str:
        stack = []
        for c in s:
            if c.isdigit() and not stack[-1].isdigit():
                stack.pop()
            elif not c.isdigit():
                stack.append(c)
            else:
                continue
        
        return ''.join(stack)
