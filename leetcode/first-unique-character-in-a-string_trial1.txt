class Solution:
    def firstUniqChar(self, s: str) -> int:
        counter = {}
        for c in s:
            counter[c] = counter.get(c,0) + 1
        
        for c in range(len(s)):
            if counter[s[c]] == 1:
                return c
        
        return -1