class Solution:
    def reductionOperations(self, nums: list[int]) -> int:
        nums.sort()
        total_ops = 0
        for i in range(1, len(nums)):
            if nums[i] > nums[i-1]:            
                total_ops += len(nums) - i 
        return total_ops